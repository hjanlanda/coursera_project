import React from 'react';
import Flex from '../components/UI/Flex';
import PageHeader from '../components/PageHeader';

const MenuPage = () => {
  return (
    <Flex direction={'column'}>
      <PageHeader title={'Menu'} description={'adsfa sdfadsf a'} />
    </Flex>
  );
};

export default MenuPage;
