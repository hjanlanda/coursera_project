import React from 'react';
import Flex from '../components/UI/Flex';
import PageHeader from '../components/PageHeader';

const LoginPage = () => {
  return (
    <Flex direction={'column'}>
      <PageHeader title={'Login'} description={'asdfasdfa'} />
    </Flex>
  );
};

export default LoginPage;
